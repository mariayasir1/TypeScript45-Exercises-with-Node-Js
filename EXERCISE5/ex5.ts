// repeat exercise 4 but this timestore person`s name in variable 
// then compose your message and store it in a ne variable called message then print your message

let hadith: string ="Know that patience is at the first strike of a calamity.";
let messenger: string= "Prophet Muhammad(s.a.w)";
let message: string = (`${messenger} once said: "${hadith}"`);

console.log(message);

